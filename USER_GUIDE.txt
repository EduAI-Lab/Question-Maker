================================================================================
                             QUESTION MAKER USER GUIDE
================================================================================
Version: 1.1 (Draft refresh)
Audience: Professors and course instructors using Question Maker

================================================================================
TABLE OF CONTENTS
================================================================================
1. Before You Start
2. Onboarding: Add Courses and Topics
3. Create Questions
   3.1 Manually
   3.2 With EduAI (AI-assisted)
4. Create Variants of a Question
   4.1 Manually
   4.2 With EduAI (AI-assisted)
5. Upload an Assessment PDF/Image → Extract Questions → Auto-Create Assessment
6. Build Assessments
   6.1 Create an Assessment Blueprint
   6.2 Edit an Assessment Blueprint
   6.3 Create Sections and Select Questions
   6.4 Create Questions/Variants inline while building a section
   6.5 Export to Canvas
   6.6 Export to TXT
7. AI Models and API Keys

================================================================================
1. BEFORE YOU START
================================================================================
- Account creation and login is required. New users are prompted to register (email/password).
- Default upon logging in is the Questions tab. Switch between Questions and Assessments via the top tabs.
- Draft vs Reviewed: Newly added variants are drafts unless you mark them reviewed (uncheck “Draft”). Drafts block exports to Canvas/TXT.

================================================================================
2. ONBOARDING: ADD COURSES AND TOPICS
================================================================================
Flow: Top nav profile icon (👤) → “Link courses from EduAI”.
1) Open the profile button in the top-right of the Landing page.
2) In “Link courses from EduAI”, pick the courses you teach.
   - “Already added” badge means it’s already in your library.
   - Each course imports its topics automatically from EduAI.
3) Click “Add selected courses”. Courses + topics are created locally.
4) (Optional) Use the “Logout” button in this dialog to end the session.
Tip: Add courses before creating questions or assessments.

================================================================================
3. CREATE QUESTIONS
================================================================================
Entry point: Questions tab → “Add Question”.

3.1 Manually
------------
1) Select a course from the top selector.
2) Click “Add Question”.
3) Fill Question Metadata:
   - Primary Topic (required)
   - Description (short label for the question)
   - Type: MCQ / SA / LA
4) Fill Variant Details (every question needs at least one variant):
   - Question Text (required)
   - Difficulty (easy/medium/hard)
   - Optional: Answer, Secondary Topics, link to an Assessment, Reference ID
   - Draft toggle: leave on to keep as draft; turn off to mark reviewed.
5) Save. The new question appears in the Questions list.

3.2 With EduAI (AI-assisted)
----------------------------
Use the right-hand “Generate with EduAI” panel inside Add Question:
1) Click “Add Question”.
2) On right-hand panel enter a prompt (topic/instructions).
3) Choose a model:
   - Internal: campus-hosted; no provider API key; data stays on campus servers.
   - External: provider-hosted (e.g., Google/Gemini, OpenAI, DeepSeek, Anthropic); requires your personal API key.
4) If external, enter your provider API key (stored locally/encrypted; never
   sent to our servers). A warning banner reminds you data leaves campus.
5) Click Generate. Review/edit the returned question text/description, assign
   topics, and set draft/reviewed status. Save as in 3.1.

================================================================================
4. CREATE VARIANTS OF A QUESTION
================================================================================
Entry points: Question detail view or “Create Variant” actions in the list.

4.1 Manually
------------
1) Open the question, choose “Create Variant”
2) Enter Variant Text, Difficulty, optional Answer/Secondary Topics/Assessment.
3) Draft toggle: leave draft on by default; turn off when reviewed.
4) Save. The variant is added to the question; question list updates.

4.2 Copy Fields from the Base Variant (quick fill)
--------------------------------------------------
1) Open the question, choose “Create Variant”
2) From the right-hand panel, click “Copy Fields” to prefill text, difficulty, answer, and secondary topics from the base variant.
3) Edit any fields as needed (and adjust draft status).
4) Save.

4.3 With EduAI (AI-assisted)
----------------------------
1) Open the question, choose “Create Variant”
2) From the right-hand panel, enter an AI prompt (e.g., “Make it harder and add an edge case”).
2) Pick model (Internal vs External + API key, same rules as 3.2).
3) Generate, then review/edit the variant text and difficulty; adjust topics.
4) Save (draft or reviewed).

================================================================================
5. UPLOAD AN ASSESSMENT PDF/IMAGE → EXTRACT QUESTIONS
================================================================================
Entry point: Questions tab → “Upload Questions”.
What it does: OCR + AI extract questions, then auto-create a new assessment and
an “Uploaded Questions” section.
Steps:
1) From the Questions tab, select the "Upload Questions" button.
2) Fill Assessment details (Type, Name, Semester) — required to save.
3) Pick AI model (UBC Hosted by default; external allowed with API key).
4) Upload PDF or image. Progress shows OCR → extraction.
5) Review extracted drafts:
   - Edit Summary, Question Text, Topics, Difficulty, Type, optional Answer.
   - Include/Exclude toggle per question; remove unwanted entries.
   - If no topics exist, a fallback topic (“Uploaded Questions”) is auto-created.
6) Click “Create Questions”. All included items become questions with draft
   variants, and a new assessment + section is created.
7) After save, you are navigated to the new assessment page.
Notes:
- Exports are blocked until draft flags are cleared.

================================================================================
6. BUILD ASSESSMENTS
================================================================================
6.1 Create an Assessment Blueprint
----------------------------------
Entry: Assessments tab → “Add Assessment”.
1) Provide Name, Type (Assignment/Lab/Quiz/Midterm/Final), Semester, Description.
2) Select Primary/Secondary/Excluded topics (seed defaults for sections).
3) Save Blueprint. It appears in the Assessments list.

6.2 Edit an Assessment Blueprint
--------------------------------
Entry: Open an assessment page → click “Edit Blueprint”.
1) Review/update Name, Type, Semester, Description.
2) Adjust Primary/Secondary/Excluded topics as needed.
3) Save Blueprint; changes apply to that assessment’s defaults.

6.3 Create Sections and Select Questions
----------------------------------------
On an assessment page:
1) Click to add a section (Create Section panel).
2) Set Section Name, desired question types, topic filters, difficulty and reasoning emphasis.
3) Run search; matching questions/variants are listed based on those filters.
4) Select variants to attach to the section.
5) Save the section; it appears in the assessment page.

6.4 Create Questions/Variants Inline During Section Building
------------------------------------------------------------
While in the builder, you can:
- “Create Question” → opens the Add Question dialog (see Section 3).
- “Create Variant” → opens the Add Variant dialog (see Section 4).
New items will appear in the search results and can be added to the section.

6.5 Export to Canvas
--------------------
Entry: Assessments page → “Export to Canvas”.
Requirements:
- All variants must be reviewed (no drafts) or the export is blocked.
- You must be an instructor in the target Canvas course.
Flow in the dialog:
1) If not connected: enter Canvas URL + Canvas API key, then connect (connection
   is stored for your user).
2) Once connected, pick a Canvas course from the list (Change Connection lets
   you reconnect with different credentials).
3) Click Export. A Canvas quiz is created with the assessment’s questions.
4) Success message shows questions created and quiz link/ID.

6.6 Export to TXT
-----------------
Entry: Assessment page → “Export to TXT”.
Requirements: At least one question and no draft variants.
Output: Downloads questions as a .txt file.

================================================================================
7. AI MODELS AND API KEYS
================================================================================
- Model picker subheaders: “Internal” and “External”.
  - Internal: Campus-hosted models (no provider API key needed; stays inside
    campus infrastructure).
  - External: Provider-hosted (e.g., Google/Gemini, OpenAI, DeepSeek,
    Anthropic). Prompts/data go to that provider.
- API keys (External only) are stored locally in your browser, encrypted; never
  sent to the backend. Use “Change” to swap/remove a key.
- Model pickers live in:
  - Add Question / Add Variant dialogs (EduAI generation panel).
  - Upload Questions dialog (extraction model).
- External selection shows a warning banner about data leaving campus systems.
